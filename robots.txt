# AGSIST robots.txt — agsist.com
User-agent: *
Allow: /

# Block non-public paths.
# NOTE (2026-06-12): /data/ is deliberately NOT disallowed. Pages render
# their content client-side from /data/*.json — blocking it stopped
# Googlebot's renderer (and AI crawlers) from seeing any live content.
# There is nothing in /data/ that isn't already painted on the pages.
Disallow: /api/
Disallow: /dev/

# Sitemaps
Sitemap: https://agsist.com/sitemap.xml
